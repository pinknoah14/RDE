# Kurly WMS 보충 운영 시스템 — v1.9 설계 계획서

**작성일**: 2026-05-24
**기준 버전**: v1.8 → v1.9
**브랜치**: `claude/review-design-docs-dzIPq`
**빌드 상태**: ✅ tsc + next build 통과 / pytest 218건 통과

---

## 변경 배경

v1.8 배포 이후 코드 감사 결과 발견된 운영 영향 결함 3건과,
v1.7 단계에서 미완성으로 남아있던 화면 4건을 수정/구현한다.

```
v1.8 상태
  ✓ 핵심 흐름 동작: 업로드 → 알고리즘 → 웨이브 → 검수 → Slack
  ✗ 검수 화면 보충지번 단일 표시
  ✗ today_sales 0 고정 (당일 판매량 미계산)
  ✗ Slack 메시지에 proximity_score 미표시
  ✗ 이벤트/피킹지번 화면 "구현 예정" placeholder
  ✗ 작업자 일괄 초기화 버튼 미노출

v1.9 변경
  🔴 운영 결함 3건 수정
  🟡 미완성 화면 4건 구현
  🟢 마이너 수정 (버전 1.9.0, 등)
  + 신규 회귀 테스트 7건
```

---

## 1. 🔴 운영 결함 수정

### 1-1. 보충지번 목록 노출

**문제**
- `ReplenishCandidate.matched_bins_json`은 JSON 배열 문자열로 저장됨 (FEFO 매칭 결과)
- 그러나 `GET /candidates` 응답은 모델 그대로 반환 → 프론트는 단일 `replenish_bin`만 사용
- 검수 화면에서 복수 지번이 보이지 않음

**해결**

**`backend/app/api/waves.py` — `list_candidates`**
```python
rows = session.exec(q.order_by(ReplenishCandidate.risk_score.desc())).all()
result = []
for c in rows:
    d = c.model_dump()
    try:
        d["matched_bins"] = json.loads(c.matched_bins_json or "[]")
    except Exception:
        d["matched_bins"] = []
    result.append(d)
return result
```

**`frontend/src/types/index.ts`**
```typescript
export interface MatchedBin {
  replenish_bin: string;
  allocated_qty: number;
  deadline_days?: number | null;
  receipt_date?: string | null;
  proximity_score?: number | null;
}

export interface Candidate {
  // ... 기존 필드 ...
  matched_bins: MatchedBin[];   // 신규
}
```

**`frontend/src/components/waves/CandidateCard.tsx`**
- 기존 단일 표시 → FEFO 순서 목록 렌더링
- 각 항목: `1. [지번] N개 D-7 🟢`

---

### 1-2. today_sales 실제 계산

**문제**
- `ReplenishCandidate.today_sales`는 기본값 0
- `run_algorithm()`에서 한 번도 채워주지 않음 → 검수 화면 "오늘 판매: 0" 고정

**해결**

**`backend/app/services/wave_builder.py`** — 후보 생성/갱신 직전 추가
```python
from datetime import date as date_type
from app.models.sku import DailySalesHistory

today_row = session.exec(
    select(DailySalesHistory).where(
        DailySalesHistory.sku_id == sku_id,
        DailySalesHistory.center_cd == center_cd,
        DailySalesHistory.sales_date == date_type.today(),
    )
).first()
today_sales = today_row.sales_qty if today_row else 0
```

`existing` 갱신 분기와 신규 INSERT 분기 모두에 `today_sales=today_sales` 전달.

> 주의: `DailySalesHistory`는 `sales_qty` 필드 사용 (킥오프 문서의 `total_qty`는 오기).

---

### 1-3. slack_service.py 미완성 코드 수정

**문제**
```python
box_count = loc.allocated_qty // max(1, 1)  # 항상 allocated_qty와 동일 — 무의미
score_text = ""                              # proximity_score 미표시
```

**해결**

**`backend/app/services/slack_service.py`**
```python
PROXIMITY_ICON = {4: "🟢", 3: "🟠", 2: "🟡", 1: "⚪"}

def build_task_block(task, locations):
    loc_lines = []
    for loc in locations:
        days_str = f"D-{loc.sales_deadline_days}" if loc.sales_deadline_days is not None else ""
        score_icon = PROXIMITY_ICON.get(loc.proximity_score or 0, "")
        line = f"• `{loc.replenish_bin}` {loc.allocated_qty}개"
        if days_str: line += f" {days_str}"
        if score_icon: line += f" {score_icon}"
        loc_lines.append(line)
    locations_text = "\n".join(loc_lines) if loc_lines else "⚠️ 보충지번 정보 없음"
    # ...
```

---

## 2. 🟡 미완성 화면 구현

### 2-1. 이벤트 관리 API + 화면

**신규 파일**: `backend/app/api/events.py`
**라우터 등록**: `backend/app/main.py`

| 메서드 | 경로 | 동작 |
|---|---|---|
| `GET` | `/api/v1/events` | 이벤트 목록 (시작일 내림차순) |
| `POST` | `/api/v1/events` | 이벤트 등록 |
| `PATCH` | `/api/v1/events/{event_id}` | 이벤트 수정 |
| `DELETE` | `/api/v1/events/{event_id}` | 이벤트 삭제 |

**Event 모델 적응**:
- 기존 모델은 `start_dt`/`end_dt: datetime`, `event_type` 필수
- API는 `start_date`/`end_date: "YYYY-MM-DD"` 입력 → 내부에서 datetime 변환
- `_serialize()`로 응답 정규화

**프론트 페이지**: `frontend/src/app/settings/events/page.tsx`
- 인라인 행 편집 패턴 (작업자 관리와 동일)
- 컬럼: SKU코드 / 이벤트명 / 유형 / 시작일 / 종료일 / 비고 / [편집] [삭제]

---

### 2-2. 피킹지번 관리 API + 화면

**`backend/app/api/zone_config.py`** — `picking_router` 추가
**라우터 등록**: `backend/app/main.py`

| 메서드 | 경로 | 동작 |
|---|---|---|
| `GET` | `/api/v1/picking-zones?q=` | 지번 목록 (bin_id 검색, 최대 200) |
| `POST` | `/api/v1/picking-zones` | 단건 등록 |
| `PATCH` | `/api/v1/picking-zones/{bin_id}` | 활성/비고 수정 |
| `DELETE` | `/api/v1/picking-zones/{bin_id}` | 삭제 |

**프론트 페이지**: `frontend/src/app/settings/picking-zones/page.tsx`
- 상단: 검색 입력 + [추가] 버튼
- 테이블: 지번 / 존 / 활성 체크박스 / 비고 / [삭제]

---

### 2-3. 작업자 일괄 초기화 버튼

**문제**: 백엔드 `POST /workers/daily-reset` 존재, 프론트 노출 없음.

**해결**

**`frontend/src/lib/api.ts`**
```typescript
dailyResetWorkers: () =>
  request<{ reset_count: number }>("/workers/daily-reset", { method: "POST" }),
```

**`frontend/src/app/settings/workers/page.tsx`** — 헤더에 버튼 추가
```tsx
<Button size="sm" variant="outline" onClick={handleDailyReset}>
  일괄 초기화
</Button>
```
- confirm 모달 후 `api.dailyResetWorkers()` 호출
- 결과: `초기화 완료 — N명` 토스트

---

### 2-4. 존 설정 추가/편집/삭제

**v1.8에서 이미 완료** — 본 단계에서는 별도 작업 없음.

---

## 3. 🟢 마이너 수정

| 항목 | 변경 |
|---|---|
| `backend/app/main.py` | FastAPI `version="1.7.0"` → `"1.9.0"` |
| `backend/app/api/upload.py` | `Query(regex=...)` → `Query(pattern=...)` (deprecation) |
| `backend/app/core/database.py` | v1.8에서 "Bot Token" → "Slack 봇 토큰" 완료 |
| `frontend/src/app/layout.tsx` | v1.8에서 `max-w-[1440px]` 적용 완료 |
| `frontend/src/app/settings/system/page.tsx` | v1.8에서 SYSTEM 탭 추가 완료 |

---

## 4. API 엔드포인트 변화

```
신규 (9건)
  GET    /api/v1/events
  POST   /api/v1/events
  PATCH  /api/v1/events/{id}
  DELETE /api/v1/events/{id}

  GET    /api/v1/picking-zones
  POST   /api/v1/picking-zones
  PATCH  /api/v1/picking-zones/{bin_id}
  DELETE /api/v1/picking-zones/{bin_id}

수정 (1건)
  GET    /api/v1/waves/{id}/candidates  ← matched_bins 배열 응답에 포함
```

---

## 5. 데이터 흐름 변화

```
검수 화면 — 후보 카드
  ┌─────────────────────────────────────┐
  │ [SKU명]                  [CRITICAL] │
  │ 15RA0010001 → 보충지번              │
  │   1. 15RA9990001  5개  D-7   🟢    │
  │   2. 15RA9990002  3개  D-14  🟠    │
  │ 오늘 판매: 42 일평균: 18.5         │
  └─────────────────────────────────────┘

Slack 메시지 (build_task_block 출력)
  *SKU명*
  피킹: `15RA0010001` → 보충:
  • `15RA9990001` 5개 D-7 🟢
  • `15RA9990002` 3개 D-14 🟠
```

---

## 6. 변경 파일 목록

| 파일 | 변경 유형 |
|---|---|
| `backend/app/main.py` | 수정 (라우터 + 버전) |
| `backend/app/api/events.py` | **신규** |
| `backend/app/api/zone_config.py` | 수정 (picking_router 추가) |
| `backend/app/api/waves.py` | 수정 (matched_bins 파싱) |
| `backend/app/api/upload.py` | 수정 (deprecation) |
| `backend/app/services/wave_builder.py` | 수정 (today_sales) |
| `backend/app/services/slack_service.py` | 수정 (build_task_block) |
| `backend/tests/test_v19_fixes.py` | **신규** (7건) |
| `backend/tests/test_database.py` | 수정 (bin_master 반영) |
| `frontend/src/types/index.ts` | 수정 (MatchedBin, EventItem, PickingZone) |
| `frontend/src/lib/api.ts` | 수정 (events, picking-zones, daily-reset) |
| `frontend/src/components/waves/CandidateCard.tsx` | 수정 (matched_bins 목록) |
| `frontend/src/app/settings/events/page.tsx` | 전면 재작성 |
| `frontend/src/app/settings/picking-zones/page.tsx` | 전면 재작성 |
| `frontend/src/app/settings/workers/page.tsx` | 수정 (daily-reset 버튼) |

---

## 7. 검증 결과

```
✅ TypeScript 에러: 0건
✅ Next.js build: 통과
✅ Python import: OK (app.version=1.9.0)
✅ pytest 전체: 218 passed (기존 211 + 신규 7)

신규 테스트 (test_v19_fixes.py)
  ✅ test_candidate_matched_bins_exposed
  ✅ test_today_sales_from_daily_history
  ✅ test_slack_block_has_proximity_icon
  ✅ test_event_crud
  ✅ test_picking_zone_create_read_delete
  ✅ test_zone_create_update_delete
  ✅ test_daily_reset_clears_active
```

---

## 8. 다음 버전 고려 사항

- **v2.0 후보**: 이벤트와 알고리즘 연동 — `Event.start_dt/end_dt` 범위 내 SKU에 자동 `event_flag` 마킹
- 피킹지번 CSV 일괄 등록 엔드포인트 (`POST /picking-zones/bulk`)
- 작업자 zone_access JSON 배열을 멀티셀렉트 UI로 편집
